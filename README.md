# TechCare

Estrutura inicial do projeto.

## Estrutura

- `frontend/` - interface (HTML, CSS, JS, assets)
- `backend/` - codigo do servidor / API
- `database/` - scripts e modelos de banco
- `data/` - dados brutos (raw/) e processados (processed/)
- `docs/` - documentacao
