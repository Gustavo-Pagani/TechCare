import {svg} from './common.js';
export function method(){const steps=[['chart','1. Coleta de dados','IBGE, Prefeitura, CMI, saúde e assistência','green'],['gauge','2. Tratamento','limpeza, seleção e padronização','sky'],['map','3. Integração territorial','cruzamento por APG e organização em base única','orange'],['target','4. Geração de indicadores','população, demanda, pressão e cenários','purple']];return `<section class="hero-call"><span class="round-icon mint">${svg('book')}</span><div><h2>Visão geral da metodologia</h2><p>A plataforma integra dados demográficos, territoriais e assistenciais para apoiar o planejamento público do envelhecimento em Campinas, permitindo identificar necessidades, priorizar territórios e simular cenários.</p></div><div class="integrated"><b>${svg('shield')} <span>Base do protótipo</span></b><p>• Geometria oficial das APGs<br>• Indicadores assistenciais demonstrativos<br>• Estrutura pensada para dados reais</p></div></section><section class="panel"><h2>Etapas de construção dos indicadores</h2><p>Da coleta dos dados à geração de indicadores para o planejamento.</p><div class="method-steps">${steps.map(s=>`<div class="method-step ${s[3]}"><span class="round-icon ${s[3]}">${svg(s[0])}</span><h3>${s[1]}</h3><p>${s[2]}</p></div>`).join('')}</div></section><div class="two-cols lower"><section class="panel"><h2>Composição do índice de pressão territorial</h2><p>O índice combina diferentes dimensões que refletem o envelhecimento no território.</p><div class="rings">${['Crescimento da população','Concentração 80+','Densidade de idosos','Idosos por ILPI'].map((x,i)=>`<div class="ring r${i}"><b>25%</b><span>${x}</span></div>`).join('')}</div></section><section class="panel"><h2>Fontes dos dados</h2><p>Principais fontes utilizadas na construção da plataforma.</p><div class="sources">${['IBGE — Censo 2022','Fundação Seade — projeções populacionais','Prefeitura de Campinas — limites das APGs','Conselho Municipal do Idoso — ILPIs registradas','Indicadores por APG e cenários'].map((x,i)=>`<div>${x}<b>${i===4?'MODELO / CÁLCULO':'OFICIAL'}</b></div>`).join('')}</div></section></div><div class="method-note">ⓘ　 O índice indica prioridade territorial. Não representa, sozinho, déficit exato de vagas ou fila de espera.</div>`}
