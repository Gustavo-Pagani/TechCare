import {svg} from '../pages/common.js';
const nav=[['reason','home','Razão do Projeto'],['overview','chart','Visão Geral'],['profiles','users','Perfis de Cuidado'],['territory','map','Análise por APG'],['solutions','heartPulse','Soluções Públicas'],['method','book','Metodologia']];
export function shell({current,year,content}){const ys=[2022,2030,2040,2050];return `<div class="app-shell"><aside class="sidebar"><div class="brand"><div class="brand-mark">TC</div><div><b>TechCare</b><span>Campinas 60+</span></div></div><nav>${nav.map(([id,ic,l])=>`<button class="nav-item ${id===current?'active':''}" data-page="${id}"><i>${svg(ic)}</i><span>${l}</span></button>`).join('')}</nav><div class="side-card"><b>${svg('shield')} <span>TechCare</span></b><p>Por uma Campinas mais inclusiva, saudável e preparada para a longevidade.</p><hr><small>PLANEJAMENTO HOJE.<br>MAIS QUALIDADE DE VIDA AMANHÃ.</small></div></aside><main class="workspace"><header class="topbar"><div><span class="eyebrow">PLANEJAMENTO PÚBLICO · CAMPINAS, SP</span><h1>${title(current)}</h1>${sub(current)}</div>${current==='profiles'?`<div class="header-actions"><span class="city-pill">${svg('map')} <span>Campinas · planejamento público</span></span><span class="data-pill">DADOS SIMULADOS</span></div>`:`<div class="header-actions"><div class="years">${ys.map(y=>`<button data-year="${y}" class="${year===y?'selected':''}">${y}</button>`).join('')}</div><span class="data-pill">DADOS ILUSTRATIVOS</span></div>`}</header>${content}</main></div>`}
function title(p){return({reason:'Campinas está preparada para envelhecer?',overview:'Visão Geral',profiles:'Nem todo envelhecimento exige o mesmo cuidado',territory:'Análise por APG',solutions:'Soluções Públicas',method:'Metodologia'})[p]};function sub(p){const x={reason:'Dados, projeções e caminhos para o planejamento público da longevidade.',profiles:'Estimativa da distribuição da população idosa segundo autonomia funcional e necessidade de apoio.',territory:'Explore os territórios e entenda os desafios e oportunidades de cada região.',solutions:'Do diagnóstico à ação: um conjunto de soluções para uma cidade mais preparada.',method:'Como os dados foram organizados e transformados em indicadores para o planejamento do envelhecimento.'};return x[p]?`<p class="page-subtitle">${x[p]}</p>`:''}
